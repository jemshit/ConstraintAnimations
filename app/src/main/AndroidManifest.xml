<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.jemshit.constraintanimations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".LandingActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".animation_activities.OneActivity"
            android:parentActivityName=".LandingActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".animation_activities.TwoActivity"
            android:parentActivityName=".LandingActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".animation_activities.ThreeActivity"
            android:parentActivityName=".LandingActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".animation_activities.FourActivity"
            android:parentActivityName=".LandingActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".animation_activities.FiveActivity"
            android:parentActivityName=".LandingActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>